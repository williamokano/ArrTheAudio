# ArrTheAudio Environment Variables
# Copy this file to .env and fill in your values

# TMDB API Key (Phase 3 - Original Language Detection)
# Get a free API key at: https://www.themoviedb.org/settings/api
# Set to empty string or remove if tmdb.enabled=false in config
TMDB_API_KEY=your_tmdb_api_key_here

# Webhook Secret (Phase 2 - Webhook Authentication)
# Shared secret for HMAC signature verification
# Generate with: openssl rand -hex 32
# Leave empty to disable webhook authentication (not recommended)
WEBHOOK_SECRET=your_webhook_secret_here

# Optional: Timezone (for logging timestamps)
TZ=America/New_York

# Optional: User/Group IDs (for Docker)
PUID=1000
PGID=1000

# Optional: Log Level Override
# Uncomment to override config.yaml logging.level
# LOG_LEVEL=debug
